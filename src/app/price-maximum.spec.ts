import { PriceMaximum } from './price-maximum';

describe('PriceMaximum', () => {
  it('should create an instance', () => {
    const directive = new PriceMaximum();
    expect(directive).toBeTruthy();
  });
});
